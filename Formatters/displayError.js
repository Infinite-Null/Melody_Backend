const displayError = (origin, error)=>{
   console.log(`================== ${origin} error ==================`)
   console.log(error);
   console.log(`==================${origin} error ==================`)
}

module.exports = displayError